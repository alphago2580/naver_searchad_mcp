# Build artifacts
dist/
*.egg-info/
__pycache__/

# Envs / secrets
.env
.env.*
api_keys.txt

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Python cache
*.pyc
*.pyo
*.pyd

# Misc
/.mypy_cache/
/.pytest_cache/
/.ruff_cache/

# DXT build artifacts / inspection
# 모든 .dxt 추적 (배포 산출물도 이력 관리) 원하면 아래 두 줄 사용 해제 상태 유지
# 필요 시 특정 임시 산출물만 무시하려면 패턴을 세분화하세요 (예: *_temp.dxt)
dxt_extension/_inspect/

# Embedded runtime (can be re-generated)
dxt_extension/runtime/

# Local bundle venv used during vendor builds
dxt_extension/.bundle_venv/

# VSCode / tooling
.vscode/
